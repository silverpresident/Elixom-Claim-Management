# Project Memory

## Current baseline

- **Stage (2026-09-08):** Sprint 12 Standard MCP Server and Versioned Operations API is complete. `/mcp` is the authenticated, rate-limited official SDK transport, `/api/v1` is its separately scoped versioned REST API, and the six legacy MCP-labelled controllers are retired. Sprint 13 Release Fidelity and Assurance is now in progress.
- **Runtime:** .NET 10 / C# 14, ASP.NET Core MVC, EF Core, Azure SQL.
- **Database:** single-company Azure SQL database using schema `dbclaim`; money uses `decimal(18,2)`, JMD only, exact two-decimal storage/calculation with no additional rounding, and persisted instants are UTC.
- **Audit Immutability:** `dbclaim.AuditRecords` append-only trigger `TR_AuditRecords_PreventMutation` enforced at Azure SQL boundary via migration `20260903090000_AddAuditRecordAppendOnlyTrigger` and ADR 0003.
- **All-Guid Identifier Convention:** Standardized all entity primary keys and foreign key references across `dbclaim` schema to `Guid` per ADR 0004. Service DTOs, MVC route constraints (`{id:guid}`), MCP tool request schemas, `DevelopmentDataSeeder`, and test suites adopt `Guid` keys.
- **Migration baseline (2026-09-08):** Because no database has been deployed or contains preservable data, historical migrations were reset to a clean Guid-based `InitialCreate` baseline. It creates `dbclaim` and all display-number sequences before dependent tables; the following migration restores the append-only audit trigger. See `Migrations/20260908033036_InitialCreate.cs`, `20260908033045_AddAuditRecordAppendOnlyTrigger.cs`, and Sprint 12 item 5b.
- **MCP durable operations (2026-09-08):** Operations reserve an actor-scoped durable `Accepted` record before invoking an authorized salary operation; retries return that record and actor ownership is enforced on status reads. MCP outbox wake-up is now a durable request only—the hosted dispatcher remains the sole dispatch executor. See `OperationRecordService.cs`, `OperationsTools.cs`, and Sprint 12 item 5.
- **Release fidelity decision (2026-09-08):** Before production release, audit persistence will use `EntityType`, `EntityId`, and `OccurredAtUtc`; message persistence will use `To`, `From`, `Cc`, and `Bcc`. The configured system-copy recipient is Bcc-only. This data-preserving migration is planned in [Sprint 13](sprints/13-release-fidelity-and-assurance.md) and [ADR 0007](adr/0007-literal-audit-and-email-record-model.md).
- **Manager audit scope decision (2026-09-08):** Managers may list metadata-only audit records for claims, collections, and job payments. Audit records for payroll, salary, user administration, OAuth/security, email delivery, and all other domains—and all before/after state—remain Administrator-only. Enforcement is planned in [Sprint 13 item 5](sprints/13-release-fidelity-and-assurance.md). See [ADR 0007](adr/0007-literal-audit-and-email-record-model.md).
- **Outbox wake-up lifecycle (2026-09-08):** Hosted dispatch now consumes durable OutboxWakeUp requests through a lease, terminally records completion/failure, and reclaims an interrupted lease after five minutes. API/MCP remain request-only; see `OutboxWakeUpProcessor.cs`, migration `20260908074154_AddOutboxWakeUpProcessingLease`, and Sprint 12 item 5c.
- **Approved email preview ownership (2026-09-08):** The shared approved-template preview service limits Teller collection-receipt previews to records the Teller created; Manager+ retains collection operational-review scope. API and MCP adapter tests deny cross-Teller preview before recipient/body composition. See `ApprovedEmailPreviewService.cs` and Sprint 12 item 6j.
- **Payout-preview record access (2026-09-08):** A Manager may preview only a payment where they are the user payee; Accountant and Administrator roles retain settlement-wide preview access. This shared service boundary applies equally to REST and MCP and returns no body/recipient data on denial. See `ApprovedEmailPreviewService.cs`, API/MCP boundary tests, and Sprint 12 item 6n.
- **Claims payment history (2026-09-08):** Every active user sees five recent user-payee payments on My Claims and can open `/claims/payment-history` for their complete, actor-owned history. Job-management review remains all-job access for Manager and higher roles; ordinary users are payee-scoped. See `ClaimsController.cs`, `JobPaymentService.cs`, and Sprint 12 item 6o.
- **Claims dashboard/history loading (2026-09-08):** My Claims presents only the current user's unresolved non-deleted claims (Draft, Submitted, or payment-Processing). `/claims/history` returns that user's full non-deleted record in 25-record pages; it never accepts another user's identity. See `ClaimService.cs`, `ClaimsController.cs`, and Sprint 12 item 6p.
- **REST API contract (2026-09-08):** Authenticated API consumers can retrieve the generated `/openapi/v1.json` document using `api:access`; it inventories only the approved `/api/v1` surface and excludes MCP/MVC routes. See `Program.cs`, `ApiEndpointIntegrationTests.cs`, and Sprint 12 item 6k.
- **Payout notification fidelity (2026-09-08):** Settlement and approved payout-summary queueing load linked payroll entries and render them alongside transaction fees and approved adjustment context, without internal notes or full bank-account output. See `JobPaymentService.cs` and Sprint 12 item 6l.
- **REST API integration evidence (2026-09-08):** Real HTTP coverage verifies `api:access` scope separation, owned-claim reads, bounded-pagination failure, durable claim-create replay, Teller role enforcement and safe collection projections, actor-owned job-payment/operation access, approved email preview redaction, prohibited-email-template rejection with no outbox mutation, and durable operation/payroll replay behavior. The product direction remains to retain and complete the remaining approved API resources. See `ApiEndpointIntegrationTests.cs` and Sprint 12 item 6.
- **REST API extension (2026-09-08):** Product direction is to retain the API. Claims support draft creation; permissioned collection/job-payment list/detail, payroll preview/run, and actor-owned operation status are available under `/api/v1`. Collection/job MCP counterparts delegate reads through `ICollectionService`/`IJobPaymentService`, removing direct adapter EF queries. See Sprint 12 item 6 and commits `4f6242e`/`91b5cd8`.
- **Approved email queue boundary (2026-09-08):** Production MCP email queue requests now delegate recipient selection, outbox writes, idempotency, authorization, and audit to `ICollectionService.QueueReceiptAsync` or `IJobPaymentService.QueuePaymentSummaryAsync`; adapters no longer own those production mutations. See Sprint 12 item 6.
- **REST email queue (2026-09-08):** `POST /api/v1/email-templates/queue` permits only `CollectionReceipt` and `PaymentSummary` requests with an entity ID and idempotency key; it never accepts recipients or body content and delegates to the approved shared queue services. Real HTTP coverage proves collection-receipt queueing is idempotent and targets only the payor plus configured system copy. See `EmailTemplatesApiController.cs`, `ApiEndpointIntegrationTests.cs`, and Sprint 12 item 6.
- **REST operation creation (2026-09-08):** `POST /api/v1/operations` accepts only the approved durable `SalaryGeneration` and `OutboxWakeUp` commands. `IApprovedOperationService` owns authorization, actor-scoped idempotency, audit, and the prohibition on direct worker execution; status reads are restricted to the requesting actor, and repeated REST outbox-wake-up requests create one record without queueing/dispatching email. See `OperationsApiController.cs`, `ApiEndpointIntegrationTests.cs`, and Sprint 12 item 6.
- **REST command idempotency (2026-09-08):** Claim create/submit and payroll run now require `Idempotency-Key` (or `X-Idempotency-Key`), reserve actor-scoped durable operation records before execution, and return the prior operation for a replay. Completed/failed outcomes are persisted; real HTTP tests prove claim-submit and payroll-run replays perform one transition/generation. See Sprint 12 item 6.
- **Approved email preview boundary (2026-09-08):** MCP and REST approved-template previews delegate to `IApprovedEmailPreviewService`; it enforces actor role checks and returns only redacted content/recipient summaries for `CollectionReceipt` and `PaymentSummary`. Real HTTP coverage proves a collection preview excludes payor name/telephone and masks recipient email. See `ApiEndpointIntegrationTests.cs` and Sprint 12 item 6.
- **Production migration coordination (2026-09-08):** Azure SQL production startup migration execution now holds the session-scoped `sp_getapplock` resource `ElixomClaim:SchemaMigration` through the pending-migration check and migration run, preventing concurrent instances from racing. See `DatabaseMigrationExtensions.cs`.
- **Test dependency advisory (2026-09-08):** `Testcontainers.MsSql` is updated to 4.15.0, resolving transitive `SSH.NET` 2026.0.0 rather than advisory-affected 2025.1.0.
- **Dependency/build hygiene (2026-09-08):** Removed redundant `Microsoft.Extensions.Options.*` package references supplied by the ASP.NET Core framework reference and migrated the relational audit test to the non-obsolete `MsSqlBuilder(image)` constructor. Restore and the complete solution suite pass without package/build warnings (218 tests).
- **Home controller observability (2026-09-08):** `HomeController` now receives `ILogger<HomeController>` and emits structured, non-sensitive logs for anonymous/dashboard/privacy/error routes. Focused Home controller tests pass.
- **OAuth client admission (2026-09-08):** Dynamic registration now creates public PKCE-only MCP clients, returns no client secret, and grants only the persisted MCP-oriented scope allow-list. Requested scopes are checked before consent and code issuance; confidential clients must present their stored secret for code exchange and refresh. Existing clients migrate as confidential. See [ADR 0006](adr/0006-oauth-client-type-and-scope-admission.md), migration `20260908042944_AddOAuthClientTypePolicy`, and OAuth tests.
- **User-facing record numbers (2026-09-07):** Claims, claim comments, job payments, collection clients/transactions, salary definitions/adjustments, payrolls, and payroll entries retain Guid technical keys but now have durable, unique, database-sequence-backed `SequenceNo` display values. MVC, printable records, and notification subjects/bodies use these numbers; routes, foreign keys, idempotency, and audit targets remain Guid-based. See migration `20260907103000_AddUserFacingSequenceNumbers`.
- **Standard MCP transport (2026-09-07):** The host registers official `ModelContextProtocol.AspNetCore` 2.2.0 stateless Streamable HTTP via `AddMcpServer().WithHttpTransport().WithTools<…>()` and maps the sole MCP endpoint at `/mcp`. It requires the custom Bearer scheme, `mcp:access`, and the MCP rate-limit policy. The six former bespoke `/mcp/*` MVC controllers are removed. Each discovered domain tool resolves its concrete actor using `McpToolActorAccessor` and shared `IActorResolver`. A conforming in-process `McpClient` initializes the authenticated transport, discovers tools, and invokes `claims_list` under the concrete actor; missing/wrong transport scopes receive `401`/`403`. See `Program.cs`, `ApiEndpointIntegrationTests.cs`, and Sprint 12 items 3/6.
- **MCP route retirement (2026-09-08):** There is no proprietary `/mcp/*` MVC fallback: `/mcp` is exclusively the official authenticated, rate-limited transport and resource endpoints remain under `/api/v1`. `ApiEndpointIntegrationTests` verifies the former `/mcp/claims` route is not mapped. See Sprint 12 item 7.
- **MCP tool boundary (2026-09-07):** The six domain-scoped tool classes expose 14 stable attributed tool names. Each MCP invocation uses the shared actor resolver and creates a correlated `IsMcp=true` audit event; cancellation is preserved and unexpected adapter failures are redacted. Collection DTOs exclude payor email and internal processing fees. Real client transport tests prove a User cannot retrieve another actor's claim via `claims_get` and that cancellation remains `OperationCanceledException`. See `McpToolContractTests.cs`, `ApiEndpointIntegrationTests.cs`, and Sprint 12 items 4/6.
- **Collection fee authority and allocation (2026-09-07):** `CollectionService` snapshots the active client `PerTransactionFee` onto a new collection and ignores caller input; the teller MVC form no longer offers a fee field. On a collection job, `JobPaymentService` applies the client `PerJobProcessingFee` once as `ClientProcessingFee`, sums attached transaction snapshots as `TotalTxnProcessingFee`, and deducts both from `TotalPaid`. Existing transaction snapshots remain immutable. See `CollectionService`, `JobPaymentService`, their regression tests, and Sprint 12 prerequisite 5a.
- **User profile and sign-out (2026-09-07):** Active users can set an optional `DisplayName` without changing their Google-provided `FullName`; the dashboard uses it when present. Payout bank details now require branch name and a Savings or Current account type, while account-number masking and audit redaction remain in force. The shared SVG icon is plum-and-gold, and authenticated navigation exposes the anti-forgery-protected sign-out action. See migration `20260907090000_AddUserProfileDisplayAndBankFields`, `ProfileController`, `_Layout.cshtml`, and Sprint 12 item 2a.
- **Collection receipt presentation (2026-09-07):** The permitted collection print route loads the teller and renders a traditional, print-friendly receipt with payer name/email, the teller's display name (falling back to full name), and browser-local payment date/time. Payor telephone and internal processing fees remain excluded. See `CollectionsController`, `Views/Collections/Print.cshtml`, and Sprint 12 item 2b.
- **Collection free-entry snapshots (2026-09-07):** Collection entry presents client-scoped purpose/amount suggestions but accepts custom values. Matching suggestions retain optional option links; custom values are immutable `CollectionTransaction.Purpose`/`Amount` snapshots and cannot create or alter administrator-managed configuration. Browser-local payment date/time is converted to UTC before the MVC command is submitted. See migration `20260907093000_AllowCustomCollectionTransactionValues`, `CollectionService`, `Views/Collections/Create.cshtml`, and Sprint 12 item 2c.
- **Collection-client bank detail completion (2026-09-07):** Administrator-created `CollectionClientBankDetail` records now require a branch name and a Savings or Current account type; the shared service validates both, and the admin form presents the constrained selection. Development seed data is compliant. Migration `20260907100000_AddCollectionClientBankBranchAndAccountType` preserves pre-existing rows with empty values that an administrator must replace before relying on them for a new payout. See `CollectionClientAdministrationService`, `CollectionClientsAdmin/Details.cshtml`, and Sprint 12 item 2d.
- **Development collection fixtures (2026-09-07):** DevelopmentTesting retains its deterministic linked collection fixture and now adds a new non-sensitive, `Collected` walk-in `CollectionTransaction` on every development host initialization. This gives the teller queue fresh usable data without changing production data paths. See `DevelopmentDataSeeder` and `DevelopmentDataSeederTests`.
- **Job-payment presentation follow-up (2026-09-07):** MVC job-payment detail and print projections include collection transactions, deductions, linked payrolls and their entries, and applicable adjustment context. The job detail links a payroll to its accountant-only workspace record; internal notes remain excluded from print. See `JobPaymentsController`, job-payment/Payroll views, tests, and Sprint 04 follow-up evidence.
- **Job-payment collection payor details (2026-09-08):** The job-payment HTML print summary displays each linked collection's payor name and supplied email, consistent with the collection receipt. Payor telephone remains internal-only and is not printed. See `Views/JobPayments/Print.cshtml`, `JobPaymentPrintViewTests.cs`, and Sprint 12 item 6d.
- **Job-payment batch attachment (2026-09-08):** Managers can select multiple eligible claims or collections, including select-all, from the MVC job-payment attachment views. The shared `JobPaymentService` validates the entire selection, attaches it atomically, recalculates totals, and records one aggregate audit event; invalid mixed selections attach nothing. See `AttachClaimsAsync`/`AttachCollectionsAsync`, the JobPayments Razor views, `JobPaymentServiceTests`, and Sprint 12 item 6a.
- **Manager review queues (2026-09-08):** The dashboard/navigation expose claim and collection review. `/manager/collections` filters records by active collection client, offers compatible Processing jobs only, and routes attachment through `JobPaymentService`; only `Collected` records are actionable. Manager claim queues now exclude soft-deleted claims and the default Submitted queue explicitly requires `Unpaid`. See `ManagerCollectionsController`, `ClaimService`, focused controller/service tests, and Sprint 12 item 6b.
- **Durable MCP Operation Tracking:** MCP operations and idempotency keys are durably stored in the `dbclaim.OperationRecords` table via `IOperationRecordService` and migration `20260903100000_AddOperationRecordsTable`, ensuring operation tracking survives application restarts.
- **Collections schema:** `CollectionClients`, client-user assignments, client bank details, client-scoped purpose/amount options, and `CollectionTransactions` are in the `dbclaim` schema. Composite foreign keys prevent a transaction from pairing options with a different client. See `20260902214419_AddCollectionEntities`.
- **Collection configuration:** the shared `CollectionClientAdministrationService` allows active Accountants and Administrators to create, edit, disable, and re-enable collection-client records; assignments, collection options, and bank-detail configuration remain active-Administrator-only. The MVC adapter is `/admin/collection-clients`, with a Collection Clients navbar entry for both authorized roles. Its assignment picker lists only active, unassigned users and filters by name/email in the browser. All mutations emit redacted audit events. See Sprint 12 items 6c/6e and `CollectionClientAdministrationService`.
- **Collection recording:** `CollectionService` requires an active teller or above, validates active options against the selected active client, and persists the JMD collection, receipt queue records, and audit record atomically on relational providers. `EmailOutboxItems` (`20260902214751_AddEmailOutbox`) has unique idempotency keys and status scheduling fields; delivery is the next sprint item.
- **Outbox delivery:** `EmailOutboxItems` are dispatched through SMTP, Azure Communication Services, or the development fake sender. `EmailLogs` (`20260902215402_AddEmailLogs`) record every delivery/skipped outcome; failures retry with bounded exponential backoff and invalid optional payor addresses are recorded as skipped without blocking other recipients.
- **Administrator operations review (2026-09-08):** `/admin` now exposes user, audit, and email-delivery review. `/admin/audit-logs` retains its Administrator full-detail / Manager metadata-only projections; `/admin/emails` and its encoded-HTML detail view are Administrator-only and read-only. See Sprint 12 item 6h and `AdminController`.
- **Teller collections UI:** `/collections` provides the recording teller’s 24-hour queue, entry, review, controlled reissue, and print-ready HTML receipt. Print/email receipts do not include internal processing fees.
- **Retention configuration:** `Retention:FinancialRecordRetentionYears` defaults to nine and is validated at startup with a four-year minimum.
- **Rate Limiting:** Application-level rate limiting middleware (`Microsoft.AspNetCore.RateLimiting`) configured with policies for OAuth (`oauth`: 20 req/min), MCP (`mcp`: 60 req/min), and MVC (`mvc`: 100 req/min). Partition keys safely extract User ID or Remote IP without leaking sensitive information on 429 response; integration coverage proves the MCP policy's 61st request returns the safe `429` envelope.
- **Database migration risk (2026-09-07):** The reset `20260903053340_InitialCreate` migration is not a valid clean-SQL baseline: it omitted `dbclaim` creation and core table creation/order, while its designer was later changed to Guid IDs without a corresponding compatibility migration. A minimal schema creation statement is present, but clean SQL still fails at a missing `CollectionClients` FK target. Do not treat the migration ledger as release-ready; Sprint 12 prerequisite 5b must reconcile it with a data-preserving upgrade path and relational evidence.
- **Salary and payroll schema:** `SalaryDefinitions`, typed percentage/fixed `SalaryAdjustments`, salary-backed `Payrolls` with unique due-period identity, and ordered `PayrollEntries` are available. Payrolls require their source salary definition; see `20260903053340_InitialCreate` and `SalaryPayrollModelTests`.
- **Salary recurrence:** `SalaryRecurrencePlanner` is the shared, pure source of due-date eligibility. It adds months then days to `LastSalaryDate`, picks the nearest configured weekday (earlier occurrence on a tie), applies inclusive definition bounds, and suppresses existing due periods.
- **Payroll generation:** `SalaryPayrollService` requires Accountant authority and atomically creates salary-sourced generated payrolls with locked base/benefit/deduction entries, an exact total, an advanced cursor, and an audit record.
- **Payroll submission:** Accountants may add custom entries only while a payroll is generated and unlocked; negative additions cannot make net pay negative. Submission locks every entry and creates exactly one linked Processing job payment.
- **Payroll operations:** Daily generation, the Accountant Generate Now action, and constrained OAuth/MCP preview/run adapters all delegate to `ISalaryPayrollService`; unique salary due periods provide idempotency across scheduler instances.
- **Salary & payroll UI:** Accountants use `/payroll` for salary-definition creation, service-backed due/total previews, lifecycle actions, and scoped payroll audit history.
- **Payroll workspace split (2026-09-08):** `/salaries` owns salary-definition setup, adjustment management, previews, and manual generation. `/payroll` now shows only Generated/Submitted work; `/payroll/history` shows bounded Paid history. The approved model has no Cancelled status, so no lifecycle state was added. See Sprint 12 item 6g, `SalariesController`, and `PayrollController`.
- **Job management:** `JobPaymentService` is the shared Manager+ adapter for Processing-only job creation and line management. It validates compatible source ownership/state and recalculates all stored JMD totals server-side.
- **Job creation usability (2026-09-08):** Managers can start a Processing job from the visible Job Payments list, or from an Accepted/Unpaid claim review with its claimant preselected. The claim is still attached only through the existing `JobPaymentService` eligibility checks; see Sprint 12 item 6f and `JobPaymentsController`.
- **Job lifecycle:** Managers submit valid Processing payments; only Accountants can schedule Submitted payments at a UTC time. Scheduled jobs are immutable because all line commands require Processing status.
- **Settlement:** only Accountants can mark a Scheduled job paid; it atomically records payment metadata, cascades claims/collections/payrolls to their paid states, queues payout notification records, and writes an audit event.
- **Accountant queue:** `/job-payments/accountant-queue` exposes Submitted and Scheduled payments for Accountant action; lifecycle, totals, source compatibility, and settlement cascade behavior are covered by focused tests.
- **Adjustments:** ADR 0002 is implemented through linked adjustment job payments: Accountant creation with a reason, Administrator approval, and Accountant settlement. Negative adjustments are recovery receivables and original paid records remain immutable.
- **Projects:** `ElixomClaim.Lib`, `ElixomClaim.Web`, and matching Lib/Web xUnit test projects under `src/`.
- **Frontend:** Razor MVC with Bootstrap 5.3 and jQuery 3.7 from CDN only; printable documents are HTML/CSS only—PDF generation is forbidden.
- **Development testing:** when and only when the host environment is Development and `DevelopmentTesting:Enabled` is true, the application uses a named EF Core InMemory database, seeds deterministic non-sensitive records for every implemented area, and exposes a role-selectable test sign-in for active roles. The Blocked sample account is inactive and cannot sign in. See `sprints/07-development-testing.md` and `src/ElixomClaim.Web/Development/DevelopmentDataSeeder.cs`.

## Security baseline

- Browser sign-in is Google OpenID Connect and requires a pre-provisioned active user. A configured default-admin email is the bootstrap escape hatch.
- MCP is authenticated through the in-house OAuth 2.0 authorization server using dynamic client registration, Authorization Code + PKCE S256, consent, rotation/revocation, and audit trails. Calls execute as the real user and never receive an elevated MCP role.
- Mutations, OAuth security events, and MCP calls require append-only audit records. Logs/audit data must not contain credentials, access tokens, or unredacted bank data.

## Domain commitments

- Roles are hierarchical single roles: Blocked, User, Teller, Manager, Accountant, Administrator.
- Claims soft-delete and cannot be changed once accepted or rejected.
- A job payment has either a claimant or a collection client—not both—and only changes line items while Processing.
- Marking a job Paid atomically updates linked claims, collections, and payrolls and creates an idempotent notification outbox record.
- Salary generation and payment state changes are domain services called by thin hosted services/controllers/MCP tools.
- MCP tools are grouped by domain class and include constrained email composition/outbox dispatch and audited background-operation requests; they do not provide arbitrary email or direct worker execution.
- Paid job payments are immutable; corrections are separately auditable linked reversal/adjustment payments.
- Accountant and Administrator can see bank details and email bodies; Manager sees email metadata only.
- Financial, audit, and email records retain for nine years. The configured retention floor is four years.

## Delivery status

Agents must use the per-sprint `Progress` table as the item-level reservation and evidence log. This table records sprint-level state only; update it when a sprint starts or completes, or when its active item/blocker changes.

| Sprint | State | Note |
| --- | --- | --- |
| 00 Foundation | Complete | All 8 items complete. See `sprints/00-foundation.md`. |
| 01 Identity & security | Complete | All 8 items complete (including prerequisite 4a trigger immutability). See `sprints/01-identity-security.md`. |
| 02 Claims | Complete | All 5 items complete. See `sprints/02-claims.md`. |
| 03 Clearing house | Complete | All 6 items complete; build and 97 tests passed on 2026-09-02. See `sprints/03-clearing-house.md`. |
| 04 Job payments | Complete | All 8 items complete; verification recorded in `sprints/04-job-payments.md`. |
| 05 Salary & payroll | Complete | All ordered items complete; Lib/Web test evidence recorded in `sprints/05-salary-payroll.md`. |
| 06 MCP & readiness | Complete | All 9 items complete; build and 127 tests passed on 2026-09-03. See `sprints/06-mcp-release.md`. |
| 07 Development testing | Complete | Development-only in-memory sample data and role-selectable test login completed; full suite passed (130 tests) on 2026-09-03. See `sprints/07-development-testing.md`. |
| 08 MCP transport & OAuth hardening | Complete | All 5 items complete; standard MCP transport, durable operations, OAuth hardening, rate limiting, and updated threat model/security test suite (145 tests passed) on 2026-09-03. See `sprints/08-mcp-oauth-hardening.md`. |
| 09 Domain data completion | Complete | All 6 items complete; EF migration 20260903120000_DomainDataCompletion applied; build & 159 tests passed on 2026-09-03. See `sprints/09-domain-data-completion.md`. |
| 10 Web workflow completion | Complete | All 7 items complete; build & 170 tests passed on 2026-09-03. See `sprints/10-web-workflow-completion.md`. |
| 11 Deployment & release verification | Complete | Guarded production migration runner, refreshed development data, end-to-end coverage, and recorded release verification matrix (176 tests passing). See `sprints/11-deployment-and-release-verification.md`. |
| 12 Standard MCP server & API | Complete | All ordered items complete. Formatter and build passed cleanly; full solution test suite passed 236 tests on 2026-09-08. See `sprints/12-standard-mcp-and-api.md`. |
| 13 Release fidelity & assurance | In progress | Begins after Sprint 12; literal audit/email persistence, payout fidelity, protocol coverage, and independent release assurance. See `sprints/13-release-fidelity-and-assurance.md`. |

## Open decisions / risks

1. **OAuth security review:** the in-house OAuth server requires a formal threat model, interoperability suite, and independent security review before release.
   The engagement procedure and completion record are documented in [the independent OAuth/MCP review runbook](docs/runbooks/independent-oauth-security-review.md).

## Decision log

| Date | Decision | Reason |
| --- | --- | --- |
| 2026-09-02 | Treat README as the consolidated working specification; preserve `context/` as source history. | The source documents overlap and include unfinished fragments. |
| 2026-09-02 | Use a durable email outbox in addition to `EmailLogs`. | In-memory queues alone cannot guarantee reliable or non-duplicated financial notifications. |
| 2026-09-02 | Implement shared service authorization for MVC and MCP. | Ensures MCP identity inheritance cannot bypass business permissions. |
| 2026-09-02 | Use JMD as the sole currency and `decimal(18,2)` money. | Single-company operating model. |
| 2026-09-02 | Paid payments are immutable; corrections use linked reversal/adjustment records. | Protects financial history and auditability. |
| 2026-09-02 | Full bank/email-body access is Accountant/Administrator only; Managers see email metadata. | Least-privilege handling of sensitive financial and message data. |
| 2026-09-02 | Retain financial, audit, and email records for nine years; configuration may not go below four. | Business retention requirement. |
| 2026-09-02 | Build the OAuth 2.0 server in-house, including dynamic client registration. | Product requirement; see ADR 0001. |
| 2026-09-02 | Group MCP tools by domain and expose only constrained email/background-operation commands. | Keeps tool surface maintainable and prevents MCP from bypassing outbox, authorization, and audit controls. |
| 2026-09-02 | Permit any Google account that is on the active-user allow-list. | No Workspace-domain restriction. |
| 2026-09-02 | Restrict Collection Client configuration to Administrators. | Centralized control of client options, assignments, and bank details. |
| 2026-09-02 | Preserve JMD values to two decimal places with no additional rounding. | Business requirement. |
| 2026-09-02 | Apply Jamaican law to privacy/legal requirements. | Business requirement. |
| 2026-09-02 | Use `privacy@elixom.com` as the privacy and support contact. | Published privacy/support contact. |
| 2026-09-02 | Use linked partial/full accounting-only adjustments: Accountant creates, Administrator approves, Accountant settles; originals stay paid and immutable. | Approved reversal workflow; see [ADR 0002](adr/0002-reversal-adjustment-accounting.md). |
| 2026-09-03 | Enforce `AuditRecords` append-only trigger `TR_AuditRecords_PreventMutation` at Azure SQL boundary via EF migration `20260903090000_AddAuditRecordAppendOnlyTrigger` and ADR 0003. | Satisfies non-negotiable append-only audit invariant. |
| 2026-09-03 | Standardize MCP transport using `ModelContextProtocol.AspNetCore` mapped at `/mcp` with Bearer auth and `mcp:access` scope validation. Retire bespoke `/mcp/*` REST controllers per ADR 0004. | Compliance with standard MCP server specification and interoperability with conforming MCP client agents. |
| 2026-09-03 | Persist OAuth consents in dbclaim.OAuthConsents, enforce strict redirect URI shape/scheme rules during dynamic client registration and authorization, stop retaining raw authorization codes in database, and revalidate client and redirect URI on consent POST. | OAuth 2.0 hardening requirements under Sprint 09 Item 3. |
| 2026-09-03 | Standardize all entity primary keys and foreign keys to Guid across `dbclaim` schema. See [ADR 0004](adr/0004-all-guid-identifier-convention.md). | Global uniqueness, uniform Web/MCP routes, and lock-free identifier generation. |
| 2026-09-03 | Complete common auditability fields: CreatedAtUtc on options/adjustments, SentAtUtc on EmailLog, and DateOfJob & DeletedAtUtc on Claim. | Sprint 09 Item 2 domain auditability requirement. |
| 2026-09-03 | Complete Sprint 09 Domain Data Completion (Items 1-6): all-Guid identifiers, auditability fields, User bank fields/masking, clearing-house fees/notes/payor telephone, job-payment title/description/payout bank snapshots, DataAnnotations, and migration 20260903120000_DomainDataCompletion. | Sprint 09 complete. |
| 2026-09-03 | Complete Sprint 10 Item 1: Ordinary-user profile & bank details route/UI (/profile), audit logging with account number redaction, navbar link, and user dashboard payment-history section. | Sprint 10 Item 1 complete. |
| 2026-09-03 | Complete Sprint 10 Item 2: Expose DateOfJob on Claim workflows, Collection Client description/notes/fees and bank detail notes on Admin workflows, and Payor Telephone on Collection workflows. Internal fees/notes remain excluded from receipts. | Sprint 10 Item 2 complete. |
| 2026-09-03 | Complete Sprint 10 Item 3: Manager job-payment workflows for creation, payee selection (User vs Collection Client), claim/collection discovery & removal, deductions, metadata editing, and submission. | Sprint 10 Item 3 complete. |
| 2026-09-03 | Complete Sprint 10 Item 4: Accountant job-payment workflows for scheduling, settlement (marking paid), bank snapshots, outbox notifications, and linked adjustment creation & administrator approval. | Sprint 10 Item 4 complete. |
| 2026-09-03 | Complete Sprint 10 Item 5: Salary-definition adjustment management and Accountant custom payroll entry additions in ISalaryPayrollService, PayrollController, and views. | Sprint 10 Item 5 complete. |
| 2026-09-03 | Complete Sprint 10 Item 6 & Item 7: Role-aware landing page work queues in HomeController & Index view, role-tailored navbar navigation in _Layout, and integration coverage across all Sprint 10 workflows (170 tests passing). Sprint 10 complete. | Sprint 10 complete. |
| 2026-09-03 | Establish standard MCP server endpoint at /mcp (mcp:access scope) and versioned REST API at /api/v1 (api:access scope) per [ADR 0005](adr/0005-standard-mcp-and-versioned-rest-api.md). Retire legacy bespoke /mcp/* controllers. | Clean transport separation, compliance with official SDK, and scope isolation. |
| 2026-09-08 | Dynamic OAuth registration is public-client-only; enforce each client's persisted allow-list at consent/code issuance, while trusted confidential clients require a client secret at token and refresh endpoints. | Removes arbitrary scope issuance and makes the client credential policy explicit; see [ADR 0006](adr/0006-oauth-client-type-and-scope-admission.md). |
| 2026-09-08 | Use literal structured audit fields and email headers; configured system copies are Bcc-only. | Product decision for source fidelity and recipient privacy; see [ADR 0007](adr/0007-literal-audit-and-email-record-model.md) and Sprint 13. |
| 2026-09-08 | Limit Manager audit visibility to claims, collections, and job payments metadata; reserve all other domains and before/after state for Administrators. | Narrow operational scope selected for least privilege; see ADR 0007 and Sprint 13 item 5. |
| 2026-09-07 | Keep `FullName` as the identity-provider value and add a separate optional `DisplayName`; require branch name and account type for user payout bank details. | Supports a user-controlled application name without changing sign-in identity and makes payout instructions more complete. |
