# Remaining Tasks


The behaviour of the collection transaction creation page does depend alot on the selected client. Split the create view into a 2 step process where the client must be seleted first before continuing to the transaction creation page.