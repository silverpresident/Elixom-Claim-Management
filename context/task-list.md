
# Remaining Tasks

- Register and map the standard .NET MCP server transport, expose the intended domain tool classes through it, enforce `mcp:access` consistently, and explicitly retire or retain the existing bespoke `/mcp/*` REST endpoints. In so far as possible rename or reuse the  MCP rest endpoint to for a useful API endpoint.
